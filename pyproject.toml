[project]
name = "llm-infra"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yashmit", email = "yashmitvaish4454@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "asyncio>=4.0.0",
    "fastapi>=0.141.1",
]

[project.scripts]
llm-infra = "llm_infra:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
